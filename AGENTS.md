# AGENTS.md — AI開発エージェント共通ルール

このファイルは **Kiro・Antigravity・Codex・Claude Code** など、複数のAIエージェントが同一プロジェクトを開発する際に共通して参照する「開発ルール・作業方針」です。

---

## 1. 作業開始時に必ず確認するファイル

作業を始める前に、以下の順番でファイルを読んでください。

| 優先度 | ファイル | 確認目的 |
|--------|----------|----------|
| ★★★ | `STATUS.md` | 現在地・完了済み機能・未解決問題の確認 |
| ★★★ | `SPEC.md` | システム全体仕様・データ構造・フローの確認 |
| ★★ | `README.md` | プロジェクト概要・ユーザー向け機能一覧の確認 |
| ★★ | 変更対象ファイル | 実際のコードを読んでから変更を設計する |
| ★ | `AGENTS.md`（本ファイル） | 作業ルールの再確認 |

> **コードを読まずに変更を加えないこと。** 変更対象ファイルは必ず事前に全体を読む。

---

## 2. プロジェクト基本情報

| 項目 | 内容 |
|------|------|
| アプリ名 | POKECARD |
| 現在バージョン | 2.0.0 |
| 動作環境 | **iPhone Safari 専用**（他ブラウザは考慮不要） |
| 技術スタック | Vanilla JS (ES6) + HTML/CSS のみ。外部ライブラリは jsQR のみ |
| フレームワーク | なし（バンドラー・ビルドツール・テストフレームワークなし） |
| データ永続化 | `localStorage` のみ（バックエンドなし） |
| ホスティング想定 | GitHub Pages（静的配信） |

---

## 3. コード変更の基本ルール

### 3-1. 変更前に必ずやること
- 変更対象ファイルを全文読む
- 変更が既存機能に影響するか確認する
- 関連ファイル（呼び出し元・呼び出し先）も確認する

### 3-2. 変更範囲を最小に保つ
- 指示された機能・修正のみを実装する
- 指示に含まれない既存コードのリファクタリングは行わない
- 「ついでに整理」「読みやすくするために」という理由でのコード変更は禁止
- 新しいファイルは指示がない限り作成しない

### 3-3. 既存仕様を守る
- `SPEC.md` に記載のデータ構造（localStorageキー名・JSON構造）は勝手に変更しない
- localStorageキーはバージョンサフィックス（`_v1`）ごと維持する
- 既存のHTML要素IDは変更しない（JSが依存している）
- 既存の関数・クラスのシグネチャを変更しない（他のファイルから参照されている）

### 3-4. スタイル変更のルール
- `style.css` に追加する場合はファイル末尾に追記する
- 既存のCSS変数（`--bg-main`, `--color-primary` 等）を使用する
- 既存セレクターの定義を変更する場合は影響範囲を確認する

---

## 4. iPhone Safari 固有の注意事項

このプロジェクトは **iPhone Safari 専用** です。以下を守ってください。

| 項目 | ルール |
|------|--------|
| 高さ指定 | `100vh` は使用禁止。必ず `100dvh` を使用 |
| モーダル高さ | `vh` は使用禁止。`svh` または `dvh` を使用 |
| スクロール制御 | `overscroll-behavior` を適切に設定する |
| フォント拡大 | `-webkit-text-size-adjust: 100%` を維持する |
| タップ遅延 | `touch-action: manipulation` を維持する |
| safe-area | `env(safe-area-inset-bottom)` の使用箇所を維持・追加時も対応する |
| フォント読み込み | Google Fonts は外部CDNから読み込み中（オフライン時は代替フォントにフォールバック） |

---

## 5. ファイル別の担当責務

各ファイルの責務範囲を把握し、責務外の変更を行わないこと。

| ファイル | 責務 | 変更時の注意 |
|----------|------|-------------|
| `index.html` | 全HTML構造・モーダル定義 | 既存IDを変更しない。スクリプト読み込み順を変えない |
| `css/style.css` | 全スタイル定義 | 追加は末尾へ。既存クラスの削除・改名は禁止 |
| `js/app.js` | 画面描画・イベント管理・画面遷移 | `window.renderApp` のグローバル公開を維持する |
| `js/storage.js` | localStorage CRUD・データモデル | キー名・データ構造を変えない。`window.storageManager` を維持 |
| `js/stamp.js` | アニメーション・サウンド | `window.showCelebration`・`window.showRewardCelebration`・`window.soundEffects` を維持 |
| `js/qr.js` | QRスキャン・URLパラメータ処理 | `window.qrManager` を維持 |
| `sw.js` | Service Worker・キャッシュ管理 | `CACHE_NAME` を変更する場合はバージョンを上げる |

---

## 6. 管理者パスワードの取り扱い

- 管理者パスワードはソースコード内に平文で存在するため、外部に公開・転載しない
- パスワードをコメントやドキュメント、外部ファイルに書き出さない
- パスワードの変更を依頼された場合は、既存の定数のみ変更する
- パスワードそのものをAIの回答やコミットメッセージに出力しない

---

## 7. テスト・確認のルール

このプロジェクトにはテストフレームワークがありません。変更後は以下を口頭・目視で確認・明示してください。

- [ ] 変更対象の機能が仕様通りに動作するか確認したか
- [ ] 既存の関連機能が壊れていないか確認したか（特にlocalStorage読み書き）
- [ ] iPhone Safari での表示崩れが発生しないか（dvh/safe-area等）
- [ ] Service Worker のキャッシュに影響する変更の場合、`sw.js` の `CACHE_NAME` を更新したか

> 実機確認ができない環境の場合は「実機未確認」と明示すること。

---

## 8. バージョン管理

- 機能追加・変更を行った場合は `index.html` 内のバージョンバッジを更新する
  ```html
  <span class="version-badge">Ver X.X.X (変更内容の短い説明)</span>
  ```
- `SPEC.md` の「変更履歴」テーブルに新バージョンを追記する
- `sw.js` の `CACHE_NAME` を新バージョンに合わせて更新する
  ```js
  const CACHE_NAME = 'pokecard-vX.X.X';
  ```

---

## 9. 作業完了時に行うこと

1. **変更ファイルと変更内容を報告する**（ファイル名・変更点を簡潔に列挙）
2. **`STATUS.md` を更新する**（完了機能・現在の状態・次の作業を更新）
3. STATUS.mdの変更履歴は直近の作業のみ維持し、古い履歴は削除してよい
4. **バージョンを更新した場合は `SPEC.md` の変更履歴にも追記する**
5. 未解決の問題や次のエージェントへの引き継ぎ事項があれば `STATUS.md` の該当セクションに記載する

---

## 10. 他のAIへ引き継ぐ際のルール

- 引き継ぎは `STATUS.md` で行う。このファイル（`AGENTS.md`）は変更しない
- 作業途中で中断する場合は `STATUS.md` の「現在作業中」「次に行うべき作業」を必ず更新する
- 推測や不確かな情報を `STATUS.md` に書かない。確認できた事実のみ記載する
- 前のエージェントが記載した内容を無断で削除しない（更新・追記のみ）

---

## 11. 禁止事項

- `README.md` / `AGENTS.md` の内容を無断で変更すること
- `SPEC.md` の仕様を実装と乖離させたまま放置すること
- localStorageキーの名称変更（既存ユーザーのデータが失われる）
- 外部ライブラリの新規追加（`jsQR` 以外）を無断で行うこと
- `alert()` / `confirm()` 以外のダイアログ実装を無断で導入すること（既存UIとの統一のため）
